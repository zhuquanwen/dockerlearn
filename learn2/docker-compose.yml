version : '2'
services:

  springappserver:
    build:
      context: .
      dockerfile: springapp.dockerfile
    ports:
      - "8080:8080"
    volumes:
      - .:/vol/development
    links:
        - mysql:mysql

  mysql:
    image: docker.io/mysql:5.7
    ports:
      - "3306:3306"